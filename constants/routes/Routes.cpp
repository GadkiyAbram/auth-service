#include "Routes.h"

namespace Routes {
    const string DEFAULT = "/";
    const string LOGIN = "/login";
    const string ABOUT = "/about";
    const string CREATE_USER = "/users/create";
    const string VALIDATE = "/token/validate";
}
